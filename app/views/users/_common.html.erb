<h1>User info</h1>
<%= image_tag user.get_profile_image(100,100) %>
<p>name<%= user.name %></p>
<p>introduction<%= user.introduction %></p>

<%= link_to "人と歯車のロゴ", edit_user_path(current_user.id) %>

<!--ここ機能の分かれ目-->

<h1>New book</h1>
<%= form_with model: book do |f| %>
<%= render 'layouts/error_message', model: f.object %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>

  <div>
    <%= f.submit "Create Book" %>
  </div>
  <% end %>